import { colors, fontSize } from "../../constants/theme";

export const styles = {
    header: {
        width: '100%',
        alignItems: 'center'
    },
    logo: {
        width: 170,
        height: 50
    },
    title: {
        color: colors.darkGray,
        fontSize: fontSize.md,
        textAlign: 'center'
    }
};