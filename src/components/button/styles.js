import { colors, fontSize } from '../../constants/theme.js';

export const styles = {
    btn: {
        width: 300,
        height: 50,
        backgroundColor: colors.red,
        borderRadius: 6,
        marginTop: 25
    },
    textos: {
        fontSize: fontSize.md,
        color: colors.white,
        padding: 12,
        textAlign: 'center'
    }
};