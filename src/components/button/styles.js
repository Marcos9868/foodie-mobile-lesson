import { colors, fontSize } from '../../constants/theme.js';

export const styles = {
    btn: {
        width: '100%',
        height: 50,
        backgroundColor: colors.red
    },
    textos: {
        fontSize: fontSize.md,
        color: colors.white,
        padding: 12,
        textAlign: 'center'
    }
};