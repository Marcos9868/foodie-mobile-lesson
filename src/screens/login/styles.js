import { colors, fontSize } from '../../constants/theme.js'

export const styles = {
    container: {
        flex: 1,
        padding: 20,
        alignItems: 'center',
        justifyContent: 'center'
    },
    form: {
        width: '100%',
        marginBottom: 25
    },
    formGroup: {
        width: '100%',
        marginTop: 50
    }
};